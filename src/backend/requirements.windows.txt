# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database (SQLite - embedded, no external server)
sqlalchemy==2.0.25

# Vector Store (embedded mode via path=)
# NOTE: qdrant-client 1.12+ removed .search(); we use .query_points() instead
qdrant-client>=1.12.0

# LLM & Embeddings (bundled for desktop app)
# For GPU: pip install llama-cpp-python==0.2.83 --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121 (then reinstall other deps if needed)
llama-cpp-python==0.2.83
sentence-transformers>=2.3.1
huggingface-hub>=0.20.0
einops>=0.7.0

# Document Processing
pypdf==4.0.1
pdfplumber>=0.10.0
python-docx>=0.8.11

# NER & Entity Extraction
gliner>=0.2.7
transformers>=4.38.2,<5.0.0
torch==2.5.1
torchvision==0.20.1

# Graph substrate
networkx>=3.2
rustworkx>=0.14.0

# Utilities
pydantic>=2.12.0
typing-extensions>=4.12.0
pydantic-settings>=2.1.0
python-dotenv==1.0.0
aiofiles==23.2.1

# Optimization & SOTA RAG (Phase B)
flashrank>=0.2.0         # Lightweight CPU/Onnx Reranking (SOTA)
async-lru>=2.0.4         # Async caching for Graph traversal

# Phase B2: VLM Document Parsing
docling>=2.0.0           # Structure-preserving PDF parsing (tables, charts)

# Phase B3: Semantic Chunking
semantic-text-splitter>=0.18.0  # Rust-based semantic chunking (sentence boundaries)

# Phase B4: RAPTOR Hierarchical Summarization
scikit-learn>=1.3.0      # KMeans clustering for RAPTOR hierarchy

# Phase 3.0: Hybrid LLM Layer (API model support)
litellm>=1.30.0           # Universal API router (DeepSeek, MiniMax, OpenAI, etc.)

# Phase 3.0: Hybrid Retrieval (BM25 + RRF)
bm25s>=0.2.0              # Fast sparse keyword retrieval (BM25)

# Optional local retrieval helpers
duckduckgo-search>=6.0.0  # Web fallback for Retrieval Expert

# Discovery OS: Deterministic Chemistry Plugins (Phase 1)

# Discovery OS: Spectroscopy & Closed Loop (Phase 3)
nmrglue>=0.9              # JCAMP-DX (.jdx) NMR file reading, peak picking
scipy>=1.11.0             # Signal processing, peak detection (find_peaks)

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx<0.28
